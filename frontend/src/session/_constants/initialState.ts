import { SessionState } from "../_types/sessionState";

export const initialState = {
  role: undefined,
  collaboratorsList: [],
  collaborator: undefined,
  company: undefined,
  branch: undefined,
} as SessionState;
